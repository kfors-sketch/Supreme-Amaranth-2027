// /api/routes/admin.js
// Placeholder: future route modules (catalog, exports, checkout, reports, etc.)
// For now, everything not handled by visits routes falls back to router.legacy.js.

export async function handleAdminGET(_req, _res) {
  return false;
}

export async function handleAdminPOST(_req, _res) {
  return false;
}
